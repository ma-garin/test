# UI Design 検証 ToDo

`docs/UIDesign.md` の基準に対する検証結果と、次に実施する作業です。

## 検証方法と制約

- 実施済み: テンプレート、CSS、ナビゲーション定義、ページャ実装、関連する自動テストを確認。
- 実画面確認: 利用者の承認を得て直接 Playwright を使用し、1440 × 900、1024 × 900、760 × 900 で画面と操作を確認した。内蔵ブラウザと接続済み Chrome は利用できなかったため、その代替手段である。
- キャプチャは検証時に目視確認し、テスト用の一時成果物としてリポジトリ外へ退避した。コミット対象には含めない。

## 検証サマリー

- **対象範囲**: 全体ヘッダー、二階層ナビゲーション、ページヘッダー、一覧ページャ、管理・設定の見分け方、スクロール領域。
- **利用者の目標**: 参照中の案件・画面の位置を見失わず、期限・状態・優先度を比較して次の操作を判断する。
- **確認できた強み**: 親ナビの文字併記、親カテゴリからの先頭画面への遷移、青系トークンの統一、ページヘッダーと本文スクロールの分離、ページャの全体量表示。
- **確認して修正した事項**: 1024px幅で絞り込み操作が画面外へ出る問題を2列配置で解消した。表示件数セレクトの `URL is not a constructor` も `window.URL` を明示して修正した。
- **実画面で確認した事項**: モバイル時の子メニューはレール下部の「メニュー」から再表示でき、ページ全体の横スクロールは発生しない。フォーカス表示、長い子メニュー名、ヘッダー操作の折り返しも確認した。

## 確認済み（ソース・自動テスト）

- [x] 親ナビゲーションはアイコンと文字を併記している。
- [x] 親ナビゲーションはカテゴリ先頭の画面へのリンクである。
- [x] 「AIプロジェクト管制」を「プロジェクト管理」、「管制ダッシュボード」を「プロジェクトダッシュボード」へ変更している。
- [x] ページヘッダーと本文を分離し、本文だけをスクロールする構造になっている。
- [x] 本文開始位置にデスクトップ26px、モバイル20pxの余白を定義している。
- [x] ページャに表示範囲、20 / 50 / 100件切替、先頭・前後・末尾移動を実装している。
- [x] 淡青・青・濃紺の主要な文字色をコントラスト計算し、白背景または濃紺背景に対して4.91:1〜7.79:1を確認した。
- [x] `manage.py check` と、ナビゲーション・ページ送り・タスク一覧関連のテストが成功している。
- [x] 同一サイト内の画面切替で、現在の画面骨格を保持し、全画面読み込みによるちらつきを発生させない。
  - 対応結果: 同一オリジンの通常リンクとページャの表示件数切替を拡張遷移へ統一した。読み込み中はヘッダー下の細い進行線だけを表示し、取得失敗時は通常遷移へ戻す。実画面でタスク一覧 → 課題管理 → PMO支援、ブラウザの戻るを確認し、Navigation Timing は1件のまま、コンソールエラーは0件だった。

## 優先ToDo

### P0 — 実画面の受入確認

- [x] 1440px幅で「プロジェクトダッシュボード」「タスク一覧」「管理・設定」の3画面を撮影し、`UIDesign.md` の受け入れチェックリストと比較した。
  - 合格条件: ヘッダー、親ナビ、子メニュー、ページヘッダー、本文の境界が明確で、本文がページヘッダーの裏に現れない。
- [x] タスク一覧を最下部までスクロールし、ページヘッダー下の余白が維持されることを確認した。
  - 合格条件: ヘッダーが本文の表示領域を侵食せず、表だけが必要に応じて横スクロールする。
- [x] 760px以下で親ナビ・子メニュー・ページヘッダー・主ボタンを確認した。
  - 合格条件: 文字が切れず、子メニューが本文を覆う状態から戻れ、横スクロールがページ全体に発生しない。
  - 確認結果: 子メニューを閉じた後も、親レール下部の「メニュー」で再表示できる。横長表だけが 798px / 表示幅610px の横スクロールとなり、文書幅は760pxのまま維持した。

### P1 — 情報密度と操作の改善

- [x] タスク一覧を8列へ整理し、案件コードとWBSをタスク名の補足情報にまとめた。
  - 常時比較する項目は、担当・期限・進捗・状態・優先度・次アクション・操作とした。案件とWBSの詳細はタスク詳細画面で確認する。
- [x] タスク一覧のヘッダー操作を「表示と分析」の補助グループと「タスクを新規作成」の主操作に分けた。
- [x] 一覧・詳細の表示方式を決定した。
  - タスク・課題・リスクは編集・関連情報が多いため、現在の専用詳細画面を維持する。右詳細ペインは、閲覧だけを高速に繰り返すRAG検索結果または監査ログで必要性が発生した時点で導入を再検討する。
- [x] 管理・設定のページヘッダーに、管理対象と変更影響を示す専用コンテキストを追加した。

### P2 — 利用性とアクセシビリティ

- [x] すべてのリンク・ボタン・フォーム要素へ、明確なキーボードフォーカス表示を追加した。親ナビには完全なカテゴリ名のアクセシブル名を設定している。
- [x] 淡青の選択状態、青いリンク、バッジの文字色をコントラスト計測した。
  - 合格条件: 主要な5組の前景色と背景色はすべて4.5:1以上。
- [x] 子メニューの長い日本語ラベルは省略記号と完全名称のツールチップを使う実装にした。ページヘッダーの操作は小画面で折り返す。

### 実画面確認 — 完了

- [x] 親ナビ、子メニュー、表示件数セレクト、ページャをキーボードだけで操作した。
  - 合格条件: フォーカス位置が常に見え、親ナビリンクの読み上げ名がカテゴリ名と一致する。
- [x] 1024px・760pxの画面で、長い日本語の画面名・プロジェクト名・ボタン名が意味を失わずに折り返すか省略されることを確認した。
- [x] 1440px・1024px・760pxでスクリーンショットを撮影し、P0の重なり・余白・横スクロールの受入条件を確認した。
  - キーボード確認: `Tab` でブランド、テナント、案件、ログアウト、親ナビまで順に移動し、親ナビでは完全名称「プロジェクト管理」と黄色のフォーカスリングを確認。`Enter` で先頭画面のプロジェクトダッシュボードへ遷移した。
  - ページング確認: 20件表示で2ページ目に移動後、表示件数セレクトをキーボードで操作して50件へ変更し、`?per_page=50`へ遷移することを確認。ページ番号もフォーカスして `Enter` で2ページ目へ移動した。修正後のコンソールエラーは0件。

## 追加検証観点 — 業務システムとしての品質

参照画像から抽出した画面構成・余白・一覧性の観点に加え、日常的な業務利用での判断、失敗からの回復、データへの信頼を確認するための項目です。以下は未着手の追加ToDoであり、前節までの完了済み検証を取り消すものではありません。

### P0 — 判断と安全に直結する状態

- [x] 主要な3業務フローを、目的達成まで通して確認する。
  - 対象例: 案件を切り替える → 期限超過タスクを見つける → 詳細を開く、課題を登録・更新する、管理・設定を変更する。
  - 合格条件: 各段階で対象案件・現在地・次の操作・完了結果を迷わず確認できる。
  - 対応結果: 画面ヘッダーに「表示範囲」と画面表示時点を共通表示し、60件のHTTP回帰テストでタスク・課題・リスク・変更要求・案件権限を通過した。実ブラウザではタスク一覧、詳細遷移、表示件数変更、確認操作を確認した。
- [x] 読み込み中、データ0件、検索結果0件、権限不足、サーバーエラーの表示を確認する。
  - 合格条件: 何が起きたか、利用者が次に取れる操作、再試行または問い合わせ先が分かる。空の画面だけを表示しない。
  - 対応結果: 条件一致0件には条件クリアを、権限不足には403画面を、400/404/500には原因と「ログイン画面へ」「ダッシュボードへ」の復帰導線を実装・テストした。
- [x] 保存・更新・検知実行などの処理中、成功、失敗、重複実行時のフィードバックを確認する。
  - 合格条件: 処理中は二重操作を防ぎ、成功・失敗の結果と影響範囲を画面上で確認できる。
  - 対応結果: 保存フォームは送信時に `aria-busy`、送信中通知、送信ボタン無効化を行う。実ブラウザで更新フォームの送信中状態と二重送信防止を確認し、成功・失敗は既存のライブメッセージで通知する。
- [x] 削除、権限変更、外部連携、未保存のままの画面離脱を確認する。
  - 合格条件: 破壊的または高影響の操作は対象・影響・取り消し可否を事前に示し、未保存変更は意図せず失わない。
  - 対応結果: アーカイブ、課題・リスク・不具合のクローズ、変更要求・AI提案の判断、外部同期に確認文を追加した。未保存の編集は画面離脱時に確認し、外部URLを戻り先として受け付けない。
- [x] 案件・テナント・検索条件・集計期間のデータ範囲と更新時点を確認する。
  - 合格条件: 数値、一覧、AI提案のいずれにも「どの条件・いつ時点の情報か」を追跡でき、切替後に前の文脈が混在しない。
  - 対応結果: 共通ヘッダーでテナント／案件または全案件と画面表示時点を、タスク一覧で実データ更新時点を、AI提案で提案更新日時・根拠・信頼度・判断者を表示する。外部同期の鮮度も専用画面で表示する。

### P1 — 作業の効率と一貫性

- [x] 絞り込み、並び順、表示件数、ページ位置、タブ選択の保持方針を画面間で統一する。
  - 合格条件: 詳細から一覧へ戻った際に、利用者が設定した探索条件が意図どおりに保たれるか、リセットされる場合は理由が分かる。
  - 対応結果: 一覧からの作成・編集・判断・クローズ・詳細へ現在のURLを安全な `next` として渡し、処理後は同じ検索条件・タブ・ページ位置へ戻す。外部URLは拒否する。並び順は各一覧の既定順を明示し、利用者が変更できる画面ではURLに保存する方針とした。
- [x] 一覧データが多い状態で、ソート、絞り込み、ページ送り、横スクロールを確認する。
  - 合格条件: 操作中に表示順や件数が不意に変わらず、処理待ち・結果件数・現在位置を確認できる。
  - 対応結果: 37件のタスクで20件表示→2ページ目→50件表示を実ブラウザで確認した。760pxでは文書全体は760pxのまま、横長表だけが横スクロールすることを確認した。
- [x] ラベル、用語、操作語を横断して確認する。
  - 合格条件: 同じ概念に異なる名称を使わず、「登録 / 保存」「案件 / プロジェクト」などの使い分けが定義されている。
  - 対応結果: 「案件」は利用者の対象単位、「プロジェクト」は機能カテゴリまたは画面上の説明に限定する。新規は「作成」、既存値の変更は「更新」、台帳へ追加する操作は「登録」、判断を終える操作は「クローズ／確定」に統一した。
- [x] 長い案件名、担当者名、エラー文、AI生成文を用意して表示を確認する。
  - 合格条件: 途中で意味が切れず、必要な全文確認手段がある。URLや機密情報が意図せず画面・通知に露出しない。
  - 対応結果: 長いタスク名・次アクションは折り返し、ナビの短縮名には完全名称をアクセシブル名・ツールチップで付与する。外部連携は資格情報の値を画面・ログに出さず、環境変数名のみを扱う。
- [x] AI提案・自動判定の根拠、確度、更新時点、人による確定操作を確認する。
  - 合格条件: AI出力を事実と誤認せず、根拠を確認し、最終判断者が分かる。
  - 対応結果: 根拠、信頼度、提案更新日時、判断理由、判断者、修正後アクションを提示し、AIの提案には確認付きの人による一回限りの判断を要求する。

### P2 — 包摂性と回復性

- [x] 200%ズーム、ブラウザの文字拡大、狭いウィンドウで再確認する。
  - 合格条件: 主要操作と読み上げるべき情報が欠けず、横スクロールは表など必要な領域だけに限定される。
  - 対応結果: 1440px、1024px、760px、760px相当の200%表示を実ブラウザで確認した。ページ全体の横スクロールは発生せず、表コンテナだけが横スクロールする。主要操作はモバイルで34px以上を確保した。
- [x] 見出し階層、表の見出しとセルの対応、フォームのラベル・必須・エラー、アイコンの代替名称を確認する。
  - 合格条件: スクリーンリーダー利用者にも、画面の構造・入力目的・エラー箇所・現在の状態が伝わる。
  - 対応結果: `main` とH1を関連付け、現在地に `aria-current`、表見出しに `scope=col`、フォームエラーに `aria-invalid` と説明先、送信中にライブ通知を追加した。実ブラウザのアクセシビリティツリーでこれらを確認した。
- [x] タップ対象の大きさ、キーボードフォーカス順、モーダル・ドロワーのフォーカス移動を確認する。
  - 合格条件: 小さすぎる操作や、開いた画面から戻れないフォーカス移動がない。
  - 対応結果: 760pxで親レールから子メニューを再表示でき、フォーカス表示と操作対象の大きさを確認した。確認操作はブラウザ標準ダイアログを使うため、キーボードで確実に中止・確定できる。
- [x] 色覚差、ハイコントラスト、動きの抑制設定を確認する。
  - 合格条件: 色だけに依存する状態がなく、必要以上の点滅・自動移動がなく、利用者設定を尊重する。
  - 対応結果: 状態は色だけでなく文字ラベル・バッジで示す。`prefers-reduced-motion` と `forced-colors` のCSSを追加し、点滅や自動移動は使用しない。

## 既存範囲の完了条件

- [x] P0の全項目を実画面で確認し、結果をこの文書に追記した。
- [x] P1の表の列整理と管理・設定コンテキストの方針を決定・実装した。
- [x] P2のキーボード操作とコントラストを確認し、検出した問題を修正した。

## 追加観点の完了条件

- [x] P0の状態・安全性を、実データまたは再現可能なテストデータで確認する。
- [x] P1の作業継続性・用語・AI出力の扱いを、主要業務フローごとに確認する。
- [x] P2のズーム、支援技術、入力支援、利用者設定を確認し、未対応の項目は理由と対応時期を残す。

## 追加観点の検証記録（2026-08-11）

- 自動テスト: `apps.core.tests.test_ui_quality` と案件CRUD・変更要求・不具合・課題・リスク・案件権限の計61件が成功した。
- 実画面: 内蔵ブラウザが利用できなかったため、利用者の事前承認済みの直接Playwrightで検証した。アーカイブ確認の中止、送信中の二重実行防止、ページング、0件表示、1440px／1024px／760px／200%表示、アクセシビリティツリー、コンソールエラー0件を確認した。
- 制約: Python環境には `playwright` パッケージが入っていないため、リポジトリ内の13本のPython E2Eテストは起動できなかった。直接Playwrightの実画面検証とHTTP回帰テストで代替し、アプリケーションの実装不備としては扱わない。
