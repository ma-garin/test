# このリポジトリで作業するときの約束

## トークンを浪費しない（最優先）

過去に、サブエージェントの完了を待つためにバックグラウンドで `sleep` を回し、
**数百万トークンを成果ゼロで浪費した**。原因は「待ち方」であって作業量ではない。

再発の仕組みはこうだった。

1. `sleep 599 &` を起動する
2. 完了通知でエージェント本体が再呼び出しされる
3. **再呼び出しのたびに会話全体を読み直す**（コンテキスト長ぶんの入力トークン）
4. また `sleep` を起動する → 1 へ戻る

20 回繰り返せば「コンテキスト長 × 20」が消える。成果物は 1 つも増えない。

### 守ること

- **待機目的の `sleep` を書かない。** 60 秒以上の `sleep` は
  `.claude/hooks/block-idle-wait.sh` が機械的に拒否する
- **ポーリングしない。** バックグラウンド処理とサブエージェントの完了は
  ハーネスの通知で届く。`ListAgents` や `ReadNotifications` を
  「まだかな」で叩かない
- **通知が来るまで発言しない。** 「待機中です」という発言そのものが、
  1 回ぶんのコンテキスト読み直しを発生させる
- **重い処理は最初から並列で流す。** 直列に流して待つのが最も高くつく
- **結果は数値だけ読む。** 大きな JSON をチャットへ出さない。
  `python -c` で件数だけ抜く

## サブエージェント

- **モデルは `sonnet` を指定する**（`Agent` の `model: "sonnet"`）。
  調査・実行・修整のいずれも sonnet で足りる
- **担当ファイルを重複させない。** 同じファイルを 2 体が触ると衝突する。
  起動時にプロンプトへ「触ってよいファイル」を列挙する
- **監視は常駐させる。** 作業エージェントを起動したら、その作業を監視する
  エージェントも同時に起動し、**逸脱をその場で検知させる**。
  全部終わってから一括で点検しても、やり直しの費用が大きすぎる
- **git 操作を禁じる。** commit / push は本体がまとめて行う

## このプロジェクト固有

- 自動実行（CI・定期実行）は置かない。手元で明示的に走らせる
- 認可は `apps/accounts/services/permissions.py` の `require()` に集約する。
  書き込み・承認のビューは冒頭で必ず 1 行呼ぶ
- 秘密値は保存時に暗号化し（`apps/core/services/secrets.py`）、
  画面とログへはマスク済みの値だけを出す
- ユースケーステストの進め方は `docs/systemtest/README.md`
