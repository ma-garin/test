# Design QA — ナビゲーションとページ送り更新

## 比較対象

- 参照画像: `/Users/fujimagariyuki/Downloads/IMG_8178.HEIC`〜`IMG_8212.HEIC`
- 読み取ったデザイン意図: 親・子を分けた二階層ナビゲーション、意味のあるアイコン、選択範囲とヘッダーを結ぶ一貫した色、情報量を抑えた余白と強弱、読める行幅、独立したスクロール領域、先頭・末尾と表示件数を含む一覧ページ送り。
- 実装対象: `templates/layouts/base.html`、`templates/partials/pager.html`、`static/css/app.css`、`apps/core/pagination.py`、`static/fonts/MaterialSymbolsOutlined-subset.woff2`
- 実装スクリーンショット: 利用者承認済みの直接 Playwright で 1440 × 900、1024 × 900、760 × 900 をキャプチャし目視確認した。テスト用キャプチャはリポジトリ外の一時保管であり、コミット対象ではない。
- 想定ビューポート: デスクトップ 1440px、ノートPC幅 1024px、モバイル 760px。
- 状態: テスト用ログイン済みの一覧画面。37件のタスクを20件表示に切り替え、実際のページ送りを確認した。

## 実装済みの比較ポイント

1. **階層とレイアウト** — 色付き 80px の親カテゴリレールと白い子メニューパネルに分離し、参照画像の二階層構造を反映した。親カテゴリを選ぶと、そのカテゴリの最上部の画面へ移動する。
2. **アイコンと選択状態** — 親レールはアイコンと短いカテゴリ名（プロジェクト / PMO / ナレッジ / 監査 / 設定）を併記し、完全名称はツールチップと子メニューパネルで示す。5カテゴリに Material Symbols（`dashboard` / `support_agent` / `folder` / `history` / `settings`）を割り当てた。サブセット済みの WOFF2 を同梱し、表示時に外部通信しない。
3. **色の一貫性** — 白地に濃紺と明快なブルーを基本とし、親レール、ヘッダー下線、ブランドマーク、案件選択状態を同じ青系トークンで統一した。管理・設定も青みを残した別トーンで区別する。
4. **ページ送り** — 先頭・前へ・ページ番号・次へ・末尾を追加し、範囲外の番号には省略記号を表示する。
5. **一覧密度** — 20 / 50 / 100 件の表示切替を実装し、`?per_page=` は許可値だけを受け付ける。
6. **小画面の扱い** — 760px 以下では子メニューをオーバーレイ化し、親カテゴリレールを常に残す。子メニューを閉じた後も、レール下部の「メニュー」から再表示できる。
7. **情報密度と視線誘導** — ページ見出しの説明幅を 72 文字相当に制限し、ページ見出しと本文の間に 26px の余白を確保した。見出し→主要内容→操作の順に読み進められるようにした。
8. **スクロール範囲** — 本文・子ナビ・横長テーブルを別々のスクロール領域として安定させ、スクロールバーの有無でレイアウトが横に揺れないようにした。
9. **固定ページヘッダー** — ページ見出し・説明・操作を本文のスクロール領域から分離した。ヘッダーは常に表示され、本文はその下だけをスクロールするため、本文がヘッダーの裏側へ回り込まない。

## 機能確認

- `python manage.py check`: 成功。
- ナビゲーション、ページ送り、タスク一覧、入力ルールの関連テスト: 成功。
- Material Symbols のサブセット WOFF2 と Apache-2.0 ライセンスが存在し、テンプレート・CSS に Google Fonts / gstatic の実行時 URL が無いことを確認した。
- 検証した相互作用: 親カテゴリ切替、子メニューの表示・非表示と再表示、親ナビからカテゴリ先頭画面への移動、管理画面の専用状態、ページ移動、20→50件の表示件数切替、キーボードの `Tab` / `Enter`。
- 実画面で修正した事項: 1024px幅での絞り込み操作のはみ出し、表示件数セレクトの `URL is not a constructor` エラー。

## 制約

- 内蔵ブラウザと接続済み Chrome は利用できなかったため、利用者承認済みの直接 Playwright で確認した。
- 支援技術による実際の読み上げは実施していない。アクセシブル名とキーボードフォーカスの確認までを本検証範囲とする。

## 結果

final result: passed
