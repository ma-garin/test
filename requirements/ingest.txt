# 文書取込（PDF/Office → テキスト）に必要。
-r base.txt

# PDF はページ単位でストリーム抽出できる pypdf を優先する
# （`apps/documents/services/extractors.py`）。未導入でもテキスト系は動く。
pypdf>=4.0
pymupdf>=1.24
python-docx>=1.1
python-pptx>=1.0
openpyxl>=3.1
